<?xml version="1.0" encoding="utf-8"?>

<PreferenceScreen

    xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory

        android:title="Overview">

        <CheckBoxPreference

            android:defaultValue="true"
            android:key="tomorrowClasses"
            android:summary="This will show tomorrow's classes on the overview screen"
            android:title="Show tomorrow's classes" />

    </PreferenceCategory>

    <PreferenceCategory

        android:title="Notifications">

        <CheckBoxPreference

            android:defaultValue="false"
            android:key="advancedNotification"
            android:summary="This will show the advanced notification instead of the simple one"
            android:title="Show advanced notification" />

        <ListPreference

            android:defaultValue="30"
            android:entries="@array/next_class_notification_minutes_text"
            android:entryValues="@array/next_class_notification_minutes_values"
            android:key="nextClassNotificationMinutes"
            android:summary="Choose when a notification for the next class appears"
            android:title="Next class notification timing" />

    </PreferenceCategory>

</PreferenceScreen>